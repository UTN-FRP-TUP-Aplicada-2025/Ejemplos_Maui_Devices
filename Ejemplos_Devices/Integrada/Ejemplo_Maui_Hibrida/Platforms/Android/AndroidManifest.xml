<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

	<application android:allowBackup="true"
				 android:icon="@mipmap/appicon"
				 android:roundIcon="@mipmap/appicon_round"
				 android:supportsRtl="true">
	</application>

	<!--
        ACCESS_FINE_LOCATION: ubicación precisa (GPS).
        Necesario para obtener coordenadas exactas.
    -->
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

	<!--
        ACCESS_COARSE_LOCATION: ubicación aproximada (red/wifi).
        Requerido como acompañante de FINE_LOCATION a partir de API 31.
    -->
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

	<!--
        ACCESS_BACKGROUND_LOCATION: solo si necesitás ubicación en segundo plano.
        En Android 10+ se pide SEPARADO del permiso de primer plano.
    -->
	<!-- <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> -->

	<!--
        uses-feature con required="false" permite que la app se instale
        en dispositivos SIN GPS (tablets wifi-only).
    -->
	<uses-feature android:name="android.hardware.location.gps" android:required="false" />

	<!--
        ACCESS_NETWORK_STATE: consultar el estado de conectividad del SO
        (IConnectivity / Connectivity.ConnectivityChanged).
    -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<!--
        CALL_PHONE: realizar llamadas DIRECTAS (Intent.ActionCall) sin pasar
        por el marcador. Sólo necesario para CallMode.Direct; el modo Dialer
        no lo requiere.
    -->
	<uses-permission android:name="android.permission.CALL_PHONE" />

	<uses-permission android:name="android.permission.READ_PHONE_STATE" />
	
	<!--
        Visibilidad de paquetes (Android 11+): declarar el intent del marcador
        permite resolver/abrir el dialer del SO (PhoneDialer / CallMode.Dialer).
    -->
	<queries>
		<intent>
			<action android:name="android.intent.action.DIAL" />
			<data android:scheme="tel" />
		</intent>
	</queries>

	<!--
        Camara
    -->

	<uses-permission android:name="android.permission.CAMERA" />
	<uses-permission android:name="android.permission.RECORD_AUDIO" />

	<queries>
		<intent>
			<action android:name="android.media.action.IMAGE_CAPTURE" />
		</intent>
	</queries>
	

	<uses-sdk android:minSdkVersion="25" android:targetSdkVersion="36" />

</manifest>